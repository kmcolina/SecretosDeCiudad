<div class="container col-md-7 mt-4 mb-4">
  <div class="form-book book-mars-new"  >
          <h5 class="new-post d-flex justify-content-center"> <%= link_to route_path(@route) do %>
            <i class="fas fa-chevron-left" id="back-arrow"></i>
          <% end %> <b>Reservar un guía </b></h5>
          <%= simple_form_for [@route, @booking]  do |f| %>
            <% if @booking.errors.any? %>
            <div class="errors-container">
              <ul>
                <% @booking.errors.full_messages.each do |message| %>
                  <% message %>
                <% end %>
              </ul>
            </div>
          <% end %>
          <%= f.input :check_in, label: :Fecha%>
          <div class="btn-form">
            <%= f.submit value:'Reservar', class:'btn btn-dark btn-form' %>
          </div>
          <% end %>
  </div>
</div>
