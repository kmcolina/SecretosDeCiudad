<div class="container col-md-7 mt-4 mb-4 d-flex">
  <div class="form-img" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1566344339346-7ce57083eb57?ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&ixlib=rb-1.2.1&auto=format&fit=crop&w=546&q=80);">
  </div>
  <div class="form">
      <%= simple_form_for(@place) do |f| %>
        <% if @place.errors.any? %>
          <div class="errors-container">
            <ul>
              <% @place.errors.full_messages.each do |message| %>
                <% message %>
              <% end %>
            </ul>
          </div>
        <% end %>
        <div class="form-section">
          <div class="form-flex justify-content-between mt-4">
            <h5 class="new-post"><%= link_to root_path do %>
              <i class="fas fa-chevron-left" id="back-arrow"></i>
            <% end %> <b>Editar lugar</b></h5>
            <div class="form-photo"><%= f.input :photo, input_html: {class: 'd-none'}, label: 'Agregar foto', label_html: { class: 'btn btn-dark'},required: true %></div>
          </div>
          <%= f.input :name, label: :Nombre %>
          <%= f.input :description, label: :Descripción %>
          <%= f.input :address, label: :Dirección %>
        </div>
        <div class="form-flex">
          <%= f.input :latitude, label: :Latitud %>
          <%= f.input :longitude, label: :Longitud %>
        </div>
        <div class="form-section">
          <%= f.association :route, label: :Ruta %>
        </div>
        <div class="btn-form">
          <%= f.submit value:'Actualizar', class:'btn btn-dark'%>
        </div>
      <% end %>
  </div>
</div>
